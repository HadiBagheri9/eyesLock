using System.IO;

namespace eyesLock
{
    partial class FrmMain
    {
        //byte[] iconBytes = null;
        private void CreateIconFile(string path)
        {
            // Fill

            //File.WriteAllBytes(path, iconBytes);
        }
    }
}
